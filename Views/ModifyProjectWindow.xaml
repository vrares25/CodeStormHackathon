<Window x:Class="CodeStormHackathon.Views.ModifyProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Modificare Proiect (Asistată de AI)" Height="550" Width="750"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{DynamicResource MaterialDesignDefaultFont}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Asistent Modificare Documente" FontSize="20" FontWeight="Bold" Foreground="#10B981" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="1. Selectează fișierul sursă (.txt sau .tex)" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <DockPanel>
                <Button x:Name="BtnBrowseFile" Content="Caută..." DockPanel.Dock="Right" Margin="10,0,0,0" Click="BtnBrowseFile_Click" Style="{StaticResource MaterialDesignFlatMidBgButton}"/>
                <TextBox x:Name="TxtFilePath" IsReadOnly="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" materialDesign:HintAssist.Hint="Ex: Fisa_Curenta.tex"/>
            </DockPanel>
        </StackPanel>

        <GroupBox Grid.Row="2" Header="2. Ce dorim să modificăm?" Style="{StaticResource MaterialDesignCardGroupBox}" Margin="0,0,0,20">
            <TextBox x:Name="TxtInstructions" Style="{StaticResource MaterialDesignOutlinedTextBox}" 
                     AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="150"
                     materialDesign:HintAssist.Hint="Ex: Actualizează anul la 2026, reformulează obiectivele ca să fie mai clare și asigură-te că examenul final este setat corect."/>
        </GroupBox>

        <StackPanel Grid.Row="3">
            <Button x:Name="BtnApplyChanges" Content="APLICĂ MODIFICĂRILE ȘI SALVEAZĂ" Height="45" Click="BtnApplyChanges_Click"
                    Background="#10B981" BorderBrush="#10B981" Foreground="White" materialDesign:ButtonAssist.CornerRadius="6" FontSize="14" FontWeight="Bold"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                <ProgressBar x:Name="LoadingSpinner" Style="{StaticResource MaterialDesignCircularProgressBar}" IsIndeterminate="True" Visibility="Collapsed" Width="20" Height="20" Margin="0,0,10,0"/>
                <TextBlock x:Name="StatusText" Text="Pregătit." VerticalAlignment="Center" FontStyle="Italic"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>